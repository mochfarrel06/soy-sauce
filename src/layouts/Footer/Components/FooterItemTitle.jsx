import React from "react";

function FooterItemTitle({title}) {
  return (
    <h4 className="font-semibold text-base text-secondary tracking-wide sm:text-lg">
      {title}
    </h4>
  );
}

export default FooterItemTitle;
